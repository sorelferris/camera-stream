[project]
name = "client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sorel", email = "sorel.ferris@foxmail.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
client = "client:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
